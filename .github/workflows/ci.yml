name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: ["**"]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  pre-commit:
    uses: ./.github/workflows/reflow-pre-commit.yml

  python:
    uses: ./.github/workflows/reflow-python.yml

  validate:
    uses: ./.github/workflows/validate.yml

  checks:
    if: ${{ !cancelled() }}
    needs: [pre-commit, python, validate]
    runs-on: ubuntu-latest
    steps:
      - name: Decide whether checks passed
        uses: re-actors/alls-green@05ac9388f0aebcb5727afa17fcccfecd6f8ec5fe # release/v1
        with:
          jobs: ${{ toJSON(needs) }}

  tag-release:
    if: github.ref == 'refs/heads/main'
    needs: [checks]
    concurrency:
      group: release
      cancel-in-progress: false
    uses: ./.github/workflows/reflow-tag-release.yml
    secrets: inherit

  post-release:
    if: github.ref == 'refs/heads/main' && needs.tag-release.outputs.tagged == 'true'
    needs: [tag-release]
    uses: ./.github/workflows/reflow-post-release.yml
    secrets: inherit

  publish-release:
    if: ${{ startsWith(github.ref, 'refs/tags/v') }}
    needs: [checks]
    uses: ./.github/workflows/reflow-publish-release.yml
    permissions:
      contents: write
    secrets: inherit

  all:
    name: all
    if: ${{ !cancelled() }}
    needs: [checks, tag-release, post-release, publish-release]
    runs-on: ubuntu-latest
    steps:
      - name: Decide whether all jobs passed
        uses: re-actors/alls-green@05ac9388f0aebcb5727afa17fcccfecd6f8ec5fe # release/v1
        with:
          allowed-skips: tag-release, post-release, publish-release
          jobs: ${{ toJSON(needs) }}
